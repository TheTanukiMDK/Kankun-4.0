import { Search, UserCircle2, TreePalm } from "lucide-react";
import Link from "next/link";

export default function Navbar({}) {
  return (
    <div className="flex w-full min-h-20 bg-red-500">
      <div className="flex flex-col justify-center">
        test
        <br />
        test
      </div>
    </div>
  );
}
